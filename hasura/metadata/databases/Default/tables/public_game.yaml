table:
  schema: public
  name: game
array_relationships:
  - name: users
    using:
      foreign_key_constraint_on:
        column: game_id
        table:
          schema: public
          name: user_game
