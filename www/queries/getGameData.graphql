query GetOriginalGameCode($id: uuid!) {
  user_game_by_pk(id: $id) {
    user_guesses_aggregate {
      aggregate {
        count
      }
    }
    game {
      code_complexity
      code
    }
  }
}
